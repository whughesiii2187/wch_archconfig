# ~/.config/hyprdynamicmonitors/hyprconfigs/fallback.conf
#
# Fallback: applies when no profile matches (e.g. only some externals are connected).
# Auto-configures all connected monitors with their preferred settings.

monitor=,preferred,auto,1
